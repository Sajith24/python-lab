def area(length,width):
    length=float(length)
    width=float(width)
    return length*width

def perimeter(length,width):
    length=float(length)
    width=float(width)
    return 2*(length+width)
